import { DataAccess } from './data_access';
import { BlockHeader, Block, GenesisBlock } from './types';
import { StateStore } from './state_store';
import { Slots } from './slots';
export declare const verifyBlockNotExists: (dataAccess: DataAccess, block: Block) => Promise<void>;
export declare const verifyPreviousBlockId: (block: Block, lastBlock: Block) => void;
export declare const matchGenesisBlock: (genesisBlock: GenesisBlock, block: BlockHeader) => boolean;
export declare const verifyBlockGenerator: (header: BlockHeader, slots: Slots, stateStore: StateStore) => Promise<void>;
export declare const isValidSeedReveal: (blockHeader: BlockHeader, stateStore: StateStore, numberOfValidators: number) => boolean;
export declare const verifyReward: (blockHeader: BlockHeader, stateStore: StateStore, numberOfValidators: number) => void;
