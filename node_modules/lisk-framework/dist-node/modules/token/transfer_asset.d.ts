import { Asset } from './types';
import { BaseAsset } from '../base_asset';
import { ApplyAssetContext, ValidateAssetContext } from '../../types';
export declare class TransferAsset extends BaseAsset {
    name: string;
    id: number;
    schema: {
        $id: string;
        title: string;
        type: string;
        required: string[];
        properties: {
            amount: {
                dataType: string;
                fieldNumber: number;
            };
            recipientAddress: {
                dataType: string;
                fieldNumber: number;
            };
            data: {
                dataType: string;
                fieldNumber: number;
            };
        };
    };
    private readonly _minRemainingBalance;
    private readonly _transferFixHeight;
    constructor(minRemainingBalance: bigint, transferFixHeight?: number);
    validate({ asset, header }: ValidateAssetContext<Asset>): void;
    apply({ asset, transaction, stateStore }: ApplyAssetContext<Asset>): Promise<void>;
}
