import { BlockHeader } from '@liskhq/lisk-chain';
import { Logger } from '../../logger/logger';
import { Rounds } from './rounds';
import { FixedLengthArray, RandomSeed } from './types';
interface HeadersMap {
    [key: number]: BlockHeader;
}
export declare const findPreviousHeaderOfDelegate: (header: BlockHeader, searchTillHeight: number, headersMap: HeadersMap) => BlockHeader | undefined;
export declare const generateRandomSeeds: ({ round, rounds, headers, logger, }: {
    round: number;
    rounds: Rounds;
    headers: ReadonlyArray<BlockHeader>;
    logger: Logger;
}) => FixedLengthArray<RandomSeed, 2>;
export {};
